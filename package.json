{
  "name": "@f/promise-stream",
  "description": "A stream of promises.",
  "repository": "git://github.com/micro-js/promise-stream.git",
  "version": "1.1.0",
  "license": "MIT",
  "main": "lib/index.js",
  "scripts": {
    "postversion": "git push && git push --tags && npm publish --access=public"
  },
  "dependencies": {
    "@f/defer-promise": "^1.0.0",
    "@f/foreach": "^1.2.2",
    "@f/is-promise": "^1.1.1",
    "@f/is-undefined": "^1.1.1",
    "@f/map-array": "^1.2.1",
    "@f/to-promise": "^1.1.2"
  },
  "devDependencies": {
    "tape": "^4.2.2"
  }
}
